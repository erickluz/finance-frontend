export class ItemCategory {
  check: boolean
  value: number
  name: string
  constructor(check:boolean, value: number, name: string) {
    this.check = check
    this.value = value
    this.name = name
  }
}
